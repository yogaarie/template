apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apps-cicd.fullname" . }}-test-connection"
  labels:
    {{- include "apps-cicd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apps-cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
