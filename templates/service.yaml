apiVersion: v1
kind: Service
metadata:
  name: {{ include "apps-cicd.fullname" . }}
  labels:
    {{- include "apps-cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apps-cicd.selectorLabels" . | nindent 4 }}
